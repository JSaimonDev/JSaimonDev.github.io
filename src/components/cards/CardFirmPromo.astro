---
import { type Firm } from "@/libs/data/firms";

interface Props {
  firm: Firm;
  key: string;
}

const { firm, key } = Astro.props;
---

<div class="flex gap-[20px]">
  <div
    class="w-full aspect-square h-full relative cursor-pointer rounded-full hover:scale-105 duration-300 ease-in-out"
    style={{ backgroundColor: firm.bgColor }}
  >
    <a href={`/promociones/${key}`}>
      <img
        src={firm.logoText}
        class="rounded-full shadow-lg h-full w-full relative
        object-contain"
      />
    </a>
  </div>
</div>
