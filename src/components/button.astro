---
interface Props {
  text: string;
  link: URL;
}

const { text, link } = Astro.props;
---

<a href={link} class="mb-[60px]"
  ><button class="py-[20px] px-[40px] text-3xl bg-orange text-[#FFF] rounded-xl"
    >{text}</button
  ></a
>
