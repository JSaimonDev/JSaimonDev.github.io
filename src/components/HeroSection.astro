---
import { Image } from "astro:assets";
import BreadcrumbHeader from "./BreadcrumbHero.astro";

interface Props {
  hero_image: ImageMetadata;
  title: string;
  extraClassImage?: string;
  altTitle?: string;
  altUrl?: string;
  altIndex?: number;
  isSelection?: boolean;
}

const {
  hero_image,
  title,
  extraClassImage,
  altTitle,
  altIndex,
  altUrl,
  isSelection,
} = Astro.props;
---

<div
  class="xs:h-[350px] h-[400px] min-h-[350px] overflow-hidden shadow-[0_3px_3px_0px_rgba(50,50,0,0.5)]
      relative"
>
  <div
    class="absolute z-20 text-light-grey w-full h-full flex justify-center items-center bg-[rgba(15,15,15,0.8)]
    md:bg-transparent"
  >
    <div
      class="md:max-w-[800px] lg:max-w-[980px] lg:w-[980px] xl:max-w-[1200px]
     xl:w-[1200px] md:mx-[40px] sm:mx-[30px] mx-0 w-full px-[30px] sm:px-0"
    >
      {
        isSelection ? (
          <h1 class="w-full lg:text-7xl xs:text-6xl text-4xl md:text-left text-center greatVibes mb-[20px]">
            {title}
          </h1>
        ) : (
          <h1 class="w-full lg:text-8xl xs:text-7xl text-5xl md:text-left text-center greatVibes mb-[10px]">
            {title}
          </h1>
        )
      }
      <p class="md:w-[60%] w-full mb-[20px]">
        Es muy importante conocer todos los detalles antes de elegir una empresa
        de fondeo. Cada una de ellas funciona de manera diferente, y si no lo
        tienes todo en cuenta desde un principio, es posible que tengas
        problemas en un futuro, o incluso llegues a perder la cuenta.
      </p>
      <BreadcrumbHeader
        altIndex={altIndex}
        altTitle={altTitle}
        altUrl={altUrl}
      />
    </div>
  </div>
  <div
    class="md:bg-gradient-to-r md:from-[rgba(15,15,15,1)] md:from-20% md:via-[rgba(15,15,15,0.5)]
     md:via-50% md:to-transparent md:to-80% absolute z-10 inset-0"
  >
  </div>
  <div class="h-full">
    <Image
      src={hero_image}
      alt="Hero image"
      class={`relative lg:object-contain min-w-full lg:h-auto h-full object-cover scale-110 ${extraClassImage}
      `}
    />
  </div>
</div>
